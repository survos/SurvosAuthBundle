{% extends ['base.html.twig', "SurvosAuthBundle::base.html.twig"] %}

{% block body %}

    User profile

    {{ user.email }}

    {# this could also be components or include files, etc twig:Dropbox or component(key, {data}) #}
    {% for key, authData in user.identifiers|default([]) %}
        <h3>{{ key }}</h3>
        <div>
            {{ block(key) }}
        </div>
    {% endfor %}


{#    <a href="{{ path('user_edit', user.rp) }}">Edit my profile</a>#}
{##}
{#    {{ include('user/_delete_form.html.twig') }}#}
{#    {% for provider, details in user.identifiers %}#}
{#        <h1>{{ provider }}</h1>#}
{#    {% endfor %}#}

{% endblock %}


{% block dropbox %}
    <img src="{{ authData.data.profile_photo_url }}" alt="profile photo" />
    {{ authData.data.account_type|json_encode }}
{% endblock %}
